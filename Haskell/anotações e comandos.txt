arquivos de exemplo: 
https://github.com/euzin4/2025.1/tree/main/linguagens%20de%20programa%C3%A7%C3%A3o
https://github.com/euzin4/CC-UFFS/tree/main/2026.1/linguagens%20de%20progra%C3%A7%C3%A3o

#-----NO TERMINAL/GHCI:

ghci             --abre o compilador haskell(GHCI)

comandos uteis do ghci:
:l arquivo.hs
:r              --recarrega o programa fonte atual
:t expressao    --mostra o tipo(numeral,char,etc) da funçao/expressao
:i expressao    --faz o mesmo que ':t' com mais detalhes
:e arquivo.hs   --edita o arquivo indicado, se nao indicar o nome do arquivo ira editar arquivo atualmente sendo usado
:! comando      --executa comando do sistema
:h              --lista os comandos GHCI
:q              --termina o GHCI

let nomeFuncao x = x*3   --define uma funçao diretamente pelo terminal
chr valor(numerico)      --recebe um 'int' e devolve um 'char' de acordo com a tebela ASCII

#-----NO ARQUIVO:

-- comentario de uma linha
{- comentario de varias linhas -}

module Nome where  --só pode ter 1 modulo por arquivo, o modulo deve ter o mesmo nome do arquivo, a primeira letra do nome deve ser maiúscula

lista:
[num,num,...]            --cria uma lista (só pode ser de um tipo(Num,Char,etc))
head [num,num,...]     --retorna o primeiro item da lista
tail [...,num,num]     --retorna todos os itens da lista exceto o primeiro
reverse [num,num,...]  --inverte a ordem da lista
null [num,...] ou []   --retorna um boolean dizendo se a lista é vazia
++                     --concatena listas ex: [1,2] ++ [3,4], retorna: 1234
num : [num,num,...]    --adiciona um valor no inicio da lista

and [Bool,Bool,... ]   --retorna True se todos os elementos da lista forem verdadeiros
or [Bool,Bool,...]     --retorna True se algum elemento da lista for verdadeiro
not [Bool]             --retorna um bool contrario ao elemento de entrada

if condiçao(Num,Bool) then (Num,Char) else (Num,Char)
ex:(nesse exemplo o 'res' recebe uma 'lista' e devolve 'string', o 'show' é usado para devolver a lista em formato 'string')
lst = [1,2,3]
res = if null lst
        then "vazio"
        else show lst

(1,'b')  --tupla (pode ter mais de um tipo)
fst  --retorna o primeiro elemento da tupla
snd  --retorna o segundo elemento da tupla

funcao :: entrada -> retorno  --declaraçao dos tipos de uma funçao (funcao: nome da funcao, entrada: tipo da entrada, retorno: tipo do retorno, EX: funcao :: String -> String)
funcaoMult3 x = x*3           --declaracao da funçao
funcaoSoma x y = x+y          --função que recebe mais de um parametro

variavel <- getLine  --entrada de dados pelo teclado
putStrLn "frase printada"  --print simples
putStrLn ("frase fixa " ++ variavel ++ " frase fixa")  --print com variavel

--Função Lambda:
nomeFuncao x = x*3  --funçao normal
\x -> x*3  --funçao Lambda
--os dois recebem 'x' como parametro mas a funçao lambda não da nome a funçao
funcaoSoma \x y -> x+y  --funçao lambda que recebe 2 parametros

--funçao map (aplica uma funçao a cada elemento de uma lista):
--se a funçao for "soma x = x+1" entao:
map soma [1,2,3]        --retorna [2,3,4]
ou com lambda:
map (\x->x+1) [1,2,3]   --retorna [2,3,4]

--funçoes foldl(associativa a esquerda) e foldr(associativa a direita), reduzem uma lista usando uma função binária e um valor inicial:
com soma(o elemento antes da lista é somado ao primeiro elemento da lista):
foldl (+) 0 []    --retona 0
foldl (+) 0 [1,2]  --0+1=1+2=3
com multiplicaçao(o elemento antes da lista é 1 pois qualquer coisa mult por zero é zero):
foldl (*) 1 [1,2,3,4] --1*1=1*2=2*3=6*4=24


--funçao filter, recebe uma funçao e uma lista como argumento e filta os elementos da lista para o qual a funçao dada resulta em verdadeiro:
filter par [1,8,10,48,5,-3]  --retorna os valores par da lista "[8,10,48]"

